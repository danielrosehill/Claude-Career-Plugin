# {{WORKSPACE_NAME}}

Job-search workspace provisioned from the [career plugin](https://github.com/danielrosehill/Claude-Career-Plugin).

Not a code repo — a structured Claude Code workspace for running an organised job search. All behaviour lives in the plugin's commands; this repo is the data.

Start with `/career:onboard`, drop your resume into `user-context/`, then `/career:log-role` for each opportunity.
